---
name: setup-plugin
description: 'Install and configure OpenTabs plugins from npm or local paths. Covers search, install, tab verification, security review, testing, permission configuration, and MCP client setup. Triggers on: setup plugin, install plugin, enable plugin, configure plugin, add plugin.'
---

**IMPORTANT: This file is a stub. The actual skill content is maintained in `__SKILL__.md` in this same directory.**

Before proceeding, you MUST read the file `.claude/skills/setup-plugin/__SKILL__.md` for the latest skill instructions. The content is kept separate so that updates to the skill are always picked up fresh, even if this stub was cached at the start of a session.
